// Example: tls/tls_setup
//
// Demonstrates TLS connection with CA certificate.
// SKIP: Requires a KubeMQ server with TLS enabled and CA cert file.
//
// Channel: cpp-tls.tls_setup
// Client ID: cpp-tls-tls-setup-client
//
// Run with a KubeMQ server configured with TLS.

#include <iostream>

int main() {
    std::cout << "[SKIP] This example requires a KubeMQ server with TLS enabled. "
              << "See Go equivalent for reference." << std::endl;
    return 0;
}
