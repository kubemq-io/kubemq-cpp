// Example: connection/token_auth
//
// Demonstrates connecting to a KubeMQ server with JWT authentication.
// SKIP: Requires a KubeMQ server configured for token authentication.
//
// Channel: cpp-connection.token_auth
// Client ID: cpp-connection-token-auth-client
//
// Run with a KubeMQ server configured for token authentication.

#include <iostream>

int main() {
    std::cout << "[SKIP] This example requires a KubeMQ server with token authentication enabled. "
              << "See Go equivalent for reference." << std::endl;
    return 0;
}
